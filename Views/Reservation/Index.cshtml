@model IEnumerable<CarReservation>

<ul>
    @foreach (CarReservation reservation in Model)
    {
        <li>
            <a asp-action="Details" asp-route-id="@reservation.Id">
                @reservation.CarId @reservation.CarCustomerId @reservation.StartDate @reservation.EndDate
            </a>
        </li>
    }
</ul>