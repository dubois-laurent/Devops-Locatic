@model IEnumerable<CarBrand>

<ul>
    @foreach (CarBrand brand in Model)
    {
        <li>
            <a asp-action="Details" asp-route-id="@brand.Id">
                @brand.Name
            </a>
        </li>
    }
</ul>